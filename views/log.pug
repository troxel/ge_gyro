extends layout

block content
  .container
    .form
      .row 
        .col
          h3 Gyro Acquire Log
        .col
          a(href=fullUrl) Refresh
      .row
        .col
          if log
            pre #{log}
          else
            p No log file available.
            